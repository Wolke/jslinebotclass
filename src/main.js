const EXCHANGE_RATE = 30;

let no = 20;

function handle(text) {
  // let dollar = 0;
  // dollar = text;
  // string

  let lastName = `peter`;
  let age = 30;

  let S = 1;

  let s = `abc`;
  s = 2;

  //number
  let n = text;
  n++; // n = n+1
  // n = "wwww";
  //bolean
  //true
  //false
  let beok = true;

  let ary = [1, 2, 3, `abc`, true];
  ary[0] = [123, 456];

  return n;
}

module.exports = handle;
